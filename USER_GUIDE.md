# Руководство пользователя - Игровой лаунчер

## 📖 Оглавление

1. [Быстрый старт](#быстрый-старт)
2. [Установка и настройка](#установка-и-настройка)
3. [Основные функции](#основные-функции)
4. [Дополнительные возможности](#дополнительные-возможности)
5. [Устранение неполадок](#устранение-неполадок)
6. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

## 🚀 Быстрый старт

### Минимальные требования
- **ОС**: Windows 7/10/11, Linux, macOS
- **Python**: 3.8 или новее
- **Оперативная память**: 512 МБ
- **Место на диске**: 100 МБ для лаунчера + место для игр
- **Интернет**: Стабильное соединение

### Первый запуск

1. **Скачайте лаунчер** в папку по вашему выбору
2. **Откройте командную строку** в папке лаунчера
3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```
4. **Запустите лаунчер**:
   ```bash
   python Launcher.py
   ```

### Первоначальная настройка

При первом запуске лаунчер автоматически:
- Создаст необходимые папки и файлы конфигурации
- Настроит систему логирования
- Проверит соединение с серверами обновлений
- Инициализирует систему безопасности

## ⚙️ Установка и настройка

### Полная установка

#### Шаг 1: Установка Python
```bash
# Windows: скачайте с python.org
# Ubuntu/Debian:
sudo apt update
sudo apt install python3 python3-pip

# macOS:
brew install python3
```

#### Шаг 2: Клонирование репозитория
```bash
git clone <repository-url>
cd launcher
```

#### Шаг 3: Установка зависимостей
```bash
# Основные зависимости
pip install -r requirements.txt

# Для разработчиков (дополнительно)
pip install -r requirements-dev.txt  # если существует
```

#### Шаг 4: Настройка конфигурации

Отредактируйте файл `launcher_config.ini`:

```ini
[General]
game_path = ./game
server_url = https://your-server.com/updates
check_updates_on_startup = true
auto_update = true

[Network]
enable_p2p = true
p2p_port = 8080
max_download_speed = 0  # 0 = без ограничений
timeout = 30

[Security]
verify_signatures = true
force_https = true
check_file_integrity = true

[UI]
theme = dark  # dark/light
show_advanced_stats = true
auto_hide_completed = false
```

### Конфигурационные файлы

| Файл | Назначение | Обязательный |
|------|------------|--------------|
| `launcher_config.ini` | Основные настройки | ✅ |
| `cdn_config.json` | Настройки CDN и зеркал | ❌ |
| `launcher_log_config.py` | Конфигурация логирования | ✅ |

## 🎮 Основные функции

### Интерфейс лаунчера

#### Главное окно
- **Кнопка "Играть"**: Запуск игры после проверки обновлений
- **Прогресс-бар**: Показывает прогресс загрузки/обновления
- **Статус**: Текущее состояние лаунчера
- **Меню настроек**: Доступ к расширенным настройкам

#### Панель статистики (если включена)
- **Скорость загрузки**: Текущая и средняя скорость
- **Время до завершения**: ETA для текущей загрузки  
- **История обновлений**: Логи последних операций
- **Статистика P2P**: Информация о peer-to-peer соединениях

### Базовые операции

#### Проверка обновлений
```python
# Автоматически при запуске или вручную
1. Лаунчер подключается к серверу обновлений
2. Сравнивает версии файлов
3. Определяет необходимые для загрузки файлы
4. Показывает информацию об обновлении
```

#### Загрузка обновлений
- **Обычная загрузка**: Прямо с сервера
- **Delta-обновления**: Только измененные части файлов (экономия до 70%)
- **P2P загрузка**: От других пользователей
- **CDN загрузка**: С ближайших зеркал

#### Управление загрузками
- **Пауза/Возобновление**: Кнопки появляются автоматически
- **Отмена**: Прерывание текущей загрузки
- **Приоритизация**: Автоматическая очередь файлов

### Система безопасности

#### Проверка целостности
- **Цифровые подписи**: RSA-проверка всех файлов обновлений
- **Хеши файлов**: SHA-256 проверка каждого файла
- **HTTPS соединения**: Принудительное шифрование всех передач

#### Защита от атак
- **Path traversal protection**: Защита от вредоносных путей
- **Zip bomb protection**: Защита от архивов-бомб
- **Size limits**: Ограничения на размер файлов

## 🌟 Дополнительные возможности

### P2P система

#### Включение P2P
1. В настройках включите `enable_p2p = true`
2. Настройте порт `p2p_port = 8080` (или другой свободный)
3. Убедитесь, что порт открыт в брандмауэре

#### Как работает P2P
```
1. Лаунчер анонсируется в трекере
2. Получает список других пиров
3. При загрузке сначала пытается P2P
4. Fallback на обычную загрузку при неудаче
```

#### Преимущества P2P
- ⚡ **Быстрее**: Загрузка от ближайших пиров
- 🔄 **Надежнее**: Множественные источники
- 💰 **Экономия трафика**: Для серверов разработчика

### CDN и зеркала

#### Автоматический выбор зеркала
Лаунчер автоматически:
- Проверяет здоровье всех доступных зеркал
- Выбирает самое быстрое и надежное
- Переключается на резервные при проблемах

#### Ручная настройка зеркал
Создайте файл `cdn_config.json`:
```json
{
  "mirrors": [
    {
      "url": "https://cdn1.example.com/",
      "name": "Main CDN",
      "region": "global",
      "priority": 1
    },
    {
      "url": "https://mirror.example.com/",
      "name": "Mirror Server", 
      "region": "eu",
      "priority": 2
    }
  ],
  "check_interval": 300,
  "timeout": 30
}
```

### Система отката (Rollback)

#### Автоматический откат
При неудачном обновлении:
1. Лаунчер автоматически обнаруживает проблему
2. Восстанавливает предыдущую рабочую версию
3. Уведомляет пользователя о проблеме

#### Ручной откат
1. Нажмите кнопку **"Откат"** в главном окне
2. Выберите версию для восстановления
3. Подтвердите операцию

#### Управление резервными копиями
```bash
# Резервные копии хранятся в launcher_backups/
launcher_backups/
├── backup_v1.2.0_20241201_120000.zip
├── backup_v1.1.0_20241125_100000.zip
└── backup_index.json
```

### Оптимизация производительности

#### Адаптивная оптимизация
Лаунчер автоматически:
- Определяет тип вашего соединения (broadband/mobile/slow)
- Адаптирует количество параллельных соединений
- Оптимизирует размер чанков для загрузки
- Учитывает историю производительности

#### Ручная настройка производительности
В `launcher_config.ini`:
```ini
[Performance]
max_connections = 8        # Максимум параллельных соединений
chunk_size_mb = 4         # Размер чанка в МБ
bandwidth_limit = 0       # Ограничение скорости (0 = без ограничений)
aggressive_mode = false   # Агрессивная оптимизация
```

### Расширенная статистика

#### Просмотр статистики
1. Включите `show_advanced_stats = true` в конфигурации
2. Перезапустите лаунчер
3. Откройте вкладку "Статистика"

#### Доступная информация
- **Общая статистика**: Количество загрузок, средняя скорость
- **P2P метрики**: Количество пиров, переданные/полученные данные
- **CDN статистика**: Производительность зеркал
- **История операций**: Подробный лог всех действий

## 🔧 Устранение неполадок

### Общие проблемы

#### Лаунчер не запускается
**Причины и решения:**
```bash
# Проверьте версию Python
python --version  # Должно быть 3.8+

# Переустановите зависимости
pip install --force-reinstall -r requirements.txt

# Проверьте права доступа
# Windows: запустите от имени администратора
# Linux/macOS: проверьте права на папку
```

#### Медленная загрузка
**Способы ускорения:**
1. **Включите P2P**: `enable_p2p = true`
2. **Увеличьте соединения**: `max_connections = 16`
3. **Проверьте CDN**: Убедитесь что используется ближайшее зеркало
4. **Сетевые настройки**: Отключите VPN, проверьте фаерволл

#### Ошибки цифровых подписей
**Решение:**
```ini
# Временно отключите проверку подписей (не рекомендуется)
[Security]
verify_signatures = false

# Или обновите публичные ключи
# Скачайте новый ключ с сервера разработчика
```

#### P2P не работает
**Проверьте:**
1. **Порт открыт**: Настройте роутер и фаерволл
2. **Сетевое соединение**: P2P требует входящие соединения
3. **Трекер доступен**: Проверьте подключение к трекер-серверу
4. **Достаточно пиров**: P2P требует других пользователей онлайн

### Расширенная диагностика

#### Включение подробного логирования
```python
# В launcher_log_config.py измените уровень на DEBUG
LOGGING_CONFIG = {
    'level': logging.DEBUG,  # Было INFO
    # ...
}
```

#### Проверка файлов логов
```bash
# Основные логи
tail -f logs/launcher.log

# P2P операции
tail -f logs/p2p.log

# CDN переключения  
tail -f logs/cdn.log

# Ошибки
tail -f logs/launcher_errors.log
```

#### Команды диагностики
```bash
# Проверка сетевого соединения
python -c "
import asyncio
from cdn_manager import get_cdn_manager
asyncio.run(get_cdn_manager().check_all_mirrors())
"

# Тест P2P подключения
python -c "
from p2p_distribution import P2PDistributor
p2p = P2PDistributor()
print('Peer ID:', p2p.get_peer_id())
"

# Проверка производительности
python -c "
from bandwidth_optimizer import get_network_optimizer
asyncio.run(get_network_optimizer().initialize())
print(get_network_optimizer().get_statistics())
"
```

## ❓ Часто задаваемые вопросы

### Установка и запуск

**Q: Можно ли установить лаунчер без Python?**
A: Нет, лаунчер написан на Python и требует его установки. Планируется создание standalone версии.

**Q: Какую версию Python использовать?**
A: Минимум Python 3.8, рекомендуется 3.9+ для лучшей производительности.

**Q: Работает ли на Linux/macOS?**
A: Да, лаунчер кроссплатформенный. Протестирован на Windows, Ubuntu и macOS.

### Функциональность

**Q: Что такое delta-обновления?**
A: Технология, позволяющая загружать только изменившиеся части файлов вместо полных файлов. Экономит до 70% трафика.

**Q: Безопасен ли P2P режим?**
A: Да, все P2P передачи проверяются цифровыми подписями и хешами SHA-256. Нет риска получить поврежденные файлы.

**Q: Можно ли отключить P2P?**
A: Да, установите `enable_p2p = false` в конфигурации. Лаунчер продолжит работать через обычные загрузки.

### Производительность

**Q: Почему загрузка медленная несмотря на быстрый интернет?**
A: Возможные причины:
- Сервер перегружен (P2P поможет)
- Ограничения файерволла
- Неоптимальные настройки соединений
- Проблемы с CDN зеркалами

**Q: Как ускорить загрузки?**
A: Лучшие способы:
1. Включите P2P (`enable_p2p = true`)
2. Увеличьте соединения (`max_connections = 16`)
3. Используйте SSD для временных файлов
4. Закройте другие программы, использующие интернет

**Q: Влияет ли лаунчер на производительность игры?**
A: Нет, лаунчер автоматически освобождает ресурсы при запуске игры. При необходимости можно включить режим минимального потребления ресурсов.

### Безопасность

**Q: Как проверить подлинность обновлений?**
A: Лаунчер автоматически проверяет:
- RSA цифровые подписи файлов
- SHA-256 хеши содержимого
- HTTPS сертификаты серверов
- Целостность архивов

**Q: Что делать если проверка подписи не прошла?**
A: 
1. Не устанавливайте обновление
2. Проверьте подключение к интернету
3. Обратитесь к разработчикам игры
4. Попробуйте повторить позже

**Q: Безопасно ли хранить пароли в лаунчере?**
A: Лаунчер не хранит пароли. Если игра требует авторизации, используйте официальные методы разработчика.

### Устранение проблем

**Q: Лаунчер зависает при загрузке**
A: Попробуйте:
1. Перезапустить лаунчер
2. Очистить кеш (`Настройки -> Очистить кеш`)
3. Отключить P2P временно
4. Проверить место на диске

**Q: Ошибка "Нет доступных зеркал"**
A: Проверьте:
1. Интернет соединение
2. Настройки файерволла/антивируса
3. DNS настройки
4. Время и дата на компьютере

**Q: Как сбросить настройки к умолчанию?**
A: Удалите файлы:
- `launcher_config.ini`
- `cdn_config.json`  
- Папку `launcher_cache/`

Лаунчер создаст новые файлы при следующем запуске.

## 📞 Получение помощи

### Ресурсы поддержки
- **Документация**: Файлы `*.md` в папке лаунчера
- **Логи**: Папка `logs/` содержит подробную информацию об ошибках
- **GitHub Issues**: Сообщайте о багах и предлагайте улучшения
- **Сообщество**: Форумы игры для обсуждения проблем

### Создание отчета об ошибке

При обращении в поддержку приложите:
1. **Версию лаунчера** (`python Launcher.py --version`)
2. **Файлы логов** (особенно `launcher_errors.log`)
3. **Конфигурацию** (`launcher_config.ini`)
4. **Шаги для воспроизведения** проблемы
5. **Системную информацию** (ОС, версия Python)

### Пример отчета
```
**Описание проблемы:** 
P2P загрузка не работает

**Версия лаунчера:** 
v2.0.0

**ОС:** 
Windows 10 x64

**Логи ошибок:**
[Прикрепите файл launcher_errors.log]

**Шаги воспроизведения:**
1. Включил P2P в настройках
2. Запустил проверку обновлений
3. Получаю ошибку "Нет доступных пиров"

**Дополнительная информация:**
Порт 8080 открыт в роутере
```

---

**Удачной игры! 🎮**

*Этот лаунчер постоянно развивается. Следите за обновлениями и новыми функциями.*