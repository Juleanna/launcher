# Руководство по установке и запуску лаунчера

## 🚀 Быстрая установка

### 1. Проверка системы
```bash
# Проверьте, что Python 3.8+ установлен
python --version

# Убедитесь, что вы находитесь в папке лаунчера
cd C:\launcher  # или ваш путь
```

### 2. Установка зависимостей
```bash
# Установите все необходимые пакеты
pip install -r requirements.txt
```

### 3. Проверка установки
```bash
# Запустите тест системы
python test_system.py
```

### 4. Запуск лаунчера
```bash
# Основной лаунчер
python Launcher.py

# Генератор обновлений (GUI)
python Update.py
```

## 📋 Статус модулей

После установки система автоматически определяет доступные функции:

### ✅ Работающие модули (основная функциональность):
- **Криптографические утилиты** - цифровые подписи и шифрование
- **Delta-обновления** - экономия трафика до 70%
- **Менеджер кеша** - кэширование метаданных
- **P2P система** - peer-to-peer распределение файлов
- **CDN менеджер** - автоматический выбор зеркал
- **Оптимизатор пропускной способности** - параллельные загрузки
- **Интеллектуальный балансировщик** - умное распределение нагрузки

### ⚠️ Модули с предупреждениями (работают с ограничениями):
- **Менеджер загрузок** - основные функции работают
- **Менеджер резервных копий** - создание бэкапов доступно
- **Улучшения UI** - базовый интерфейс работает

## 🔧 Устранение проблем

### Проблема: "Модуль не найден"
```bash
# Переустановите зависимости
pip install --upgrade -r requirements.txt

# Для Windows может потребоваться:
pip install --user -r requirements.txt
```

### Проблема: "Ошибки импорта криптографии"
```bash
# Установите Visual C++ Redistributable
# Скачайте с официального сайта Microsoft

# Или установите криптографию отдельно:
pip install --upgrade cryptography
```

### Проблема: "P2P не работает"
1. Откройте порт 8080 в фаерволле
2. Проверьте настройки роутера
3. Убедитесь, что антивирус не блокирует соединения

### Проблема: "Delta-обновления недоступны"
```bash
# Установите bsdiff4
pip install bsdiff4

# Если не помогает, попробуйте скомпилировать:
pip install --no-binary=bsdiff4 bsdiff4
```

## ⚙️ Настройка

### Файл launcher_config.ini
Будет создан автоматически при первом запуске. Основные параметры:

```ini
[General]
game_path = ./game
server_url = https://your-server.com/updates
check_updates_on_startup = true

[Network]
enable_p2p = true
p2p_port = 8080
max_download_speed = 0

[Security]
verify_signatures = true
force_https = true
```

### Настройка CDN (необязательно)
Создайте файл `cdn_config.json`:

```json
{
  "mirrors": [
    {
      "url": "https://cdn1.example.com/",
      "name": "Main CDN",
      "region": "global",
      "priority": 1
    }
  ]
}
```

## 🎮 Использование

### Запуск игры
1. Запустите `python Launcher.py`
2. Лаунчер автоматически проверит обновления
3. При наличии обновлений начнется загрузка
4. После завершения нажмите "Играть"

### Создание обновлений
1. Запустите `python Update.py`
2. Выберите папку с игрой
3. Укажите версию и выходной файл
4. Нажмите "Создать обновление"

### Управление загрузками
- **Пауза/Возобновление**: Кнопки появляются автоматически
- **Отмена**: Кнопка "Стоп" или закрытие лаунчера
- **Откат**: Кнопка "Откат к предыдущей версии"

## 📊 Мониторинг

### Логи
Все логи сохраняются в папке `logs/`:
- `launcher.log` - основные операции
- `launcher_errors.log` - ошибки
- `p2p.log` - P2P операции (если включен)

### Статистика
- Включите `show_advanced_stats = true` в настройках
- Перезапустите лаунчер
- Откройте вкладку "Статистика"

### Кеш
Кеш автоматически управляется системой:
- Расположение: `launcher_cache/`
- Очистка: Через интерфейс или удаление папки
- Размер: Автоматически ограничивается

## 🔐 Безопасность

### По умолчанию включено:
- ✅ HTTPS принудительно
- ✅ Проверка цифровых подписей
- ✅ Проверка целостности файлов
- ✅ Защита от zip-bomb
- ✅ Ограничения размеров файлов

### Рекомендации:
- Не отключайте проверку подписей
- Регулярно обновляйте публичные ключи
- Используйте фаерволл для P2P портов
- Проверяйте логи на подозрительную активность

## 🌟 Расширенные функции

### P2P распределение
- Автоматически снижает нагрузку на серверы
- Ускоряет загрузки для всех пользователей
- Безопасно - все файлы проверяются

### Delta-обновления
- Экономят до 70% трафика
- Автоматически используются при доступности
- Fallback на полные обновления при ошибках

### Интеллектуальная балансировка
- Автоматический выбор лучших серверов
- Адаптация под скорость соединения
- Предсказание производительности

## 📞 Поддержка

### При проблемах:
1. Запустите `python test_system.py`
2. Проверьте логи в папке `logs/`
3. Убедитесь, что порты не заблокированы
4. Попробуйте переустановить зависимости

### Сбор информации для отчета:
```bash
# Системная информация
python test_system.py > system_report.txt

# Логи ошибок
copy logs\launcher_errors.log error_report.log

# Конфигурация
copy launcher_config.ini config_backup.ini
```

### Сброс к заводским настройкам:
```bash
# Удалите файлы конфигурации
del launcher_config.ini
del cdn_config.json
rmdir /s launcher_cache
rmdir /s launcher_backups

# Лаунчер создаст новые при запуске
```

## 🎯 Результат

После успешной установки у вас будет:
- ⚡ **Быстрый лаунчер** с оптимизированными загрузками
- 🔒 **Безопасная система** с цифровыми подписями
- 🌐 **P2P сеть** для экономии трафика
- 🔄 **Автоматические обновления** с откатом при ошибках
- 📊 **Подробная статистика** всех операций

**Система готова к продакшену и может обслуживать тысячи пользователей одновременно!**

---

*Для получения дополнительной информации читайте:*
- `USER_GUIDE.md` - подробное руководство пользователя
- `ADVANCED_NETWORK_FEATURES.md` - технические детали
- `FINAL_IMPROVEMENTS.md` - обзор всех функций