# Основные зависимости для базового функционала
PyQt5>=5.15.0
requests>=2.25.0
aiohttp>=3.8.0
configparser>=5.0.0
packaging>=21.0

# Дополнительные зависимости для расширенного функционала
cryptography>=3.4.8     # Для цифровых подписей
bsdiff4>=1.2.2          # Для delta-обновлений
aiofiles>=0.8.0         # Для асинхронной работы с файлами

# Дополнительные зависимости для сетевых функций
psutil>=5.8.0           # Для мониторинга системных ресурсов
uuid>=1.30              # Для генерации уникальных идентификаторов

# Опциональные зависимости (устанавливать по необходимости)
# watchdog>=2.1.0       # Для мониторинга файловой системы